# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking
# Ref: https://nikitabobko.github.io/AeroSpace

# It's not necessary to copy all keys to your config.
# If the key is missing in your config, "default-config.toml" will serve as a fallback

# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = []

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
key-mapping.preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 5
inner.vertical =   5
outer.left =       5
outer.bottom =     5
outer.top =        5
outer.right =      5

# See https://nikitabobko.github.io/AeroSpace/guide#exec-env-vars
[exec]                   # Again, you don't need to copy all config sections to your config.
inherit-env-vars = true  # If you don't touch "exec" section,
[exec.env-vars]          # it will fallback to "default-config.toml"
PATH = '/opt/homebrew/bin:/opt/homebrew/sbin:${PATH}'

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
[mode.main.binding]

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon, backtick,
#                   leftSquareBracket, rightSquareBracket, space, enter, esc, backspace, tab
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift
# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

alt-1 = 'workspace 1'
alt-2 = 'workspace 2'
alt-3 = 'workspace 3'
alt-4 = 'workspace 4'
alt-5 = 'workspace 5'
alt-6 = 'workspace 6'
alt-7 = 'workspace 7'
alt-8 = 'workspace 8'
alt-9 = 'workspace 9'

alt-w = 'mode service'
alt-r = 'mode resize'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]

# mode prefix-keys should also works
esc = 'mode main'
enter = 'mode main'
alt-w = 'mode service'
alt-r = 'mode resize'
z = 'mode resize'

r = ['reload-config', 'mode main'] # reset layout

h = 'focus left'
j = 'focus down'
k = 'focus up'
l = 'focus right'

# in case of: you think it's in main mode now, but we just makes it works and back to main.
alt-h = ['focus left', 'mode main']
alt-j = ['focus down', 'mode main']
alt-k = ['focus up', 'mode main']
alt-l = ['focus right', 'mode main']

shift-h = ['move left', 'mode main']
shift-j = ['move down', 'mode main']
shift-k = ['move up', 'mode main']
shift-l = ['move right', 'mode main']

x = ['close', 'mode main']
shift-x = ['close-all-windows-but-current', 'mode main']
f = ['fullscreen', 'mode main']
space = [ 'layout tiles accordion', 'mode main']


tab = 'workspace --wrap-around next'
shift-tab = 'workspace --wrap-around prev'

n = ['workspace --wrap-around next', 'mode main']
p = ['workspace --wrap-around prev', 'mode main']

1 = 'workspace 1'
2 = 'workspace 2'
3 = 'workspace 3'
4 = 'workspace 4'
5 = 'workspace 5'
6 = 'workspace 6'
7 = 'workspace 7'
8 = 'workspace 8'
9 = 'workspace 9'

shift-1 = ['move-node-to-workspace 1', 'workspace 1', 'mode main']
shift-2 = ['move-node-to-workspace 2', 'workspace 2', 'mode main']
shift-3 = ['move-node-to-workspace 3', 'workspace 3', 'mode main']
shift-4 = ['move-node-to-workspace 4', 'workspace 4', 'mode main']
shift-5 = ['move-node-to-workspace 5', 'workspace 5', 'mode main']
shift-6 = ['move-node-to-workspace 6', 'workspace 6', 'mode main']
shift-7 = ['move-node-to-workspace 7', 'workspace 7', 'mode main']
shift-8 = ['move-node-to-workspace 8', 'workspace 8', 'mode main']
shift-9 = ['move-node-to-workspace 9', 'workspace 9', 'mode main']

shift-semicolon = ['exec-and-forget open -n /Applications/Alacritty.app', 'mode main']

[mode.resize.binding]

# mode prefix-keys should also works
esc = 'mode main'
enter = 'mode main'
alt-w = 'mode service'
alt-r = 'mode resize'

h = 'resize width -50'
j = 'resize height +50'
k = 'resize height -50'
l = 'resize width +50'
